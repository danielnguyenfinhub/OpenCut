[package]
name = "opencut-core"
description = "Headless project model and export pipeline for OpenCut, shared by the desktop app and the MCP server"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "opencut_core"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
