$schema: 'https://moonrepo.dev/schemas/project.json'

language: 'rust'
layer: 'library'

tasks:
  check:
    command: 'cargo check -p opencut-core'
    inputs:
      - 'src/**/*'
      - 'Cargo.toml'
      - '/Cargo.toml'
      - '/Cargo.lock'

  test:
    command: 'cargo test -p opencut-core'
    inputs:
      - 'src/**/*'
      - 'Cargo.toml'
      - '/Cargo.toml'
      - '/Cargo.lock'
